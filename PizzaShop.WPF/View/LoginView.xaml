<UserControl x:Class="PizzaShop.WPF.View.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PizzaShop.WPF.View"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Border Name="LogInBorder">
            <StackPanel HorizontalAlignment="Stretch" VerticalAlignment="Center">
                <TextBlock Text="Вход"/>
                <DockPanel>
                    <TextBlock Text="Логин: " />
                    <TextBox Name="LogLoginBox" MaxLength="50" Text="{Binding Email, UpdateSourceTrigger=PropertyChanged}"/>
                </DockPanel>
                <DockPanel>
                    <TextBlock Text="Пароль: "/>
                    <PasswordBox Name="LogPasswordBox" MaxLength="50" PasswordChanged="PasswordBox_PasswordChanged"/>
                </DockPanel>
                <Button Content="Вход" Command="{Binding LoginCommand}" 
                     Name="EnterButton" IsDefault="True" 
                     HorizontalAlignment="Center"/>
            </StackPanel>
        </Border>
    </Grid>

</UserControl>
